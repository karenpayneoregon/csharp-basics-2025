using SamplesApp.Classes.StatementsBasics;

namespace SamplesApp.Classes.Intermediate;
internal class YieldSamples
{
    /// <summary>
    /// Demonstrates the use of <c>yield return</c> by iterating over sequences of integers
    /// and human objects generated by the <see cref="BasicStatements.GetNumbersUpToTenYieldBasic(int)"/> 
    /// and <see cref="BasicStatements.GetFirstMale"/> methods.
    /// </summary>
    /// <remarks>
    /// This method showcases two examples:
    /// <list type="bullet">
    /// <item>
    /// Iterating over a sequence of integers up to a specified maximum using 
    /// <see cref="BasicStatements.GetNumbersUpToTenYieldBasic(int)"/>.
    /// </item>
    /// <item>
    /// Iterating over a collection of human objects to display the first male's details 
    /// using <see cref="BasicStatements.GetFirstMale"/>.
    /// </item>
    /// </list>
    /// </remarks>
    public static void Example1()
    {
        foreach (int number in BasicStatements.GetNumbersUpToTenYieldBasic(20))
        {
            Console.WriteLine(number);
        }

        Console.WriteLine();

        foreach (var human in BasicStatements.GetFirstMale())
        {
            Console.WriteLine($"Name: {human.FirstName} {human.LastName}");
        }
    }
}
