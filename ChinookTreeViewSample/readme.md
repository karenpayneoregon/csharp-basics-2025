# Display Hierarchical Data

An example of how to display hierarchical data using a `Spectre.Console` TreeView with data from the Chinook sample database using `Dapper` ORM.

## Notes

The database design does not store order of album releases by artist, so the albums are displayed in alphabetical order.